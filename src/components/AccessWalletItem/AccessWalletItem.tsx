import { Button } from "@mui/material";
import { useTranslation } from "react-i18next";

const AccessWalletItem = () => {
  const { t } = useTranslation();

  return <Button variant="text">Text</Button>;
};
export default AccessWalletItem;
